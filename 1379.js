// Traverse original tree using DFS to find target, while keeping track of my path in an array
// Once I've found my target, I will iterate through my array and move cloned in the appropriate direction
// Return cloned

var getTargetCopy = function(original, cloned, target) {

};